version: '1.0'
steps:
  CheckKubectl:
    title: CheckKubectl
    image: codefresh/k8s-kubectl
    commands:
      - sed -i -- \'s/BUILD_NUMBER/${{BUILD_NUMBER}}/g\' products-dep.yml"
      - kubectl config use-context cf-staging@codefresh-staging
      - kubectl apply -f products-dep.yml
